# TC-OS Documentation

---
TC-OS is a small monolithic kernel which provides essential services and basic memory protection. User programs may utilize kernel services by invoking a system call with a valid syscall ID. The `syscall` instruction tells the CPU to enable kernel mode and jump to the syscall handler address `0x006`, which is burned-in and cannot be directly modified by software. If the ID is valid, the syscall handler will add a fixed offset `0x041` to the ID. This new number will point to the appropriate entry in the syscall table that contains the memory address for the requested kernel function. If the ID is invalid, a kernel panic will be triggered.

There are 11 valid system calls, but the kernel will only perform a permission check for two of them. If a user program calls `SYS_STORE8` or `SYS_LOAD8`, the request will only be fulfilled if the address in question lies in the program memory region `0x100`-`0x2FF` (inclusive).  The region check is performed by validating the `hi2` argument (`lo8` may be arbitrary) before performing the request. A system call for the aforementioned two syscall IDs which requests to read or write memory outside of this region will trigger a kernel panic.

When the system starts, the kernel will initialize the instruction mode and stack pointer. It will then begin the process of copying kernel functions into IRAM. Afterwards, the kernel will begin initializing the operating system itself. Control will be transferred to the terminal once the early boot phase ends and the terminal ROM is copied into IRAM. 

## User-Facing Kernel Functions
| Syscall ID Symbol | Kernel Function  | What it does                                    | Args (Stack Order)     | Return (rX)                               | Registers Clobbered              | Uses Specials                    | Special Notes                                                        |
| ----------------- | ---------------- | ----------------------------------------------- | ---------------------- | ----------------------------------------- | -------------------------------- | -------------------------------- | -------------------------------------------------------------------- |
| `SYS_GETC`        | `k_getc`         | reads a char from keyboard input                | —                      | `r0` = ascii char                         | `r0`                             | —                                | direct MMIO read via `IO_KBD_GETC`                                   |
| `SYS_PUTC`        | `k_putc`         | prints one character                            | `(ascii)`              | —                                         | `r0`, SP                         | —                                | pops one value from stack and writes to `IO_TERM_PUTC`               |
| `SYS_PUTHEX8`     | `k_puthex8`      | prints 8-bit value as 2 hex chars               | `(value)`              | —                                         | `r0`, `r1`, `r2`, SP             | `SHIFT_AMT`, `TWO_TMP`           | calls `k_putc` twice; restores `SHIFT_AMT` to `1` at end             |
| `SYS_PUTS`        | `k_puts`         | prints null-terminated string                   | `(lo8, hi2)`           | —                                         | `r0`, `r1`, `r2`, SP             | `TWO_TMP`                        | sets `TWO_TMP = hi2`; prints until `0x00`                            |
| `SYS_READLINE`    | `k_readline`     | obsolete placeholder                            | —                      | —                                         | —                                | —                                | currently just `ret`                                                 |
| `SYS_CLS`         | `k_cls`          | clears terminal                                 | —                      | —                                         | `r0`                             | —                                | writes `1` to `IO_TERM_CLR`                                          |
| `SYS_STORE8`      | `k_store8`       | store 8-bit value to RAM with permission check  | `(value, lo8, hi2)`    | —                                         | `r0`, `r1`, `r2`, SP             | `TWO_TMP`, `ERR_INFO` (indirect) | only allows `hi2 = 0b01` or `0b10`; otherwise triggers error handler |
| `SYS_LOAD8`       | `k_load8`        | load 8-bit value from RAM with permission check | `(lo8, hi2)`           | `r0` = loaded byte                        | `r0`, `r1`, SP                   | `TWO_TMP`, `ERR_INFO` (indirect) | only allows `hi2 = 0b01` or `0b10`; otherwise triggers error handler |
| `SYS_CMP_STR`     | `k_cmp_strings`  | compare two strings                             | `(lo8_s1, lo8_s2)`     | `r0 = 0` if same<br>`r0 = 1` if different | `r0`, `r1`, `r2`, `r3`, `r4`, SP | `TWO_TMP`                        | forces `TWO_TMP = 0b01`; compares until mismatch or null             |
| `SYS_SAVE_REGS`   | `k_save_regs`    | saves full register file to memory              | `(r0 value on stack)`  | —                                         | `r0`, SP                         | —                                | stores `SV_REG_0..SV_REG_7`; preserves values in memory              |
| `SYS_ASCII_2HEX`  | `k_ascii_to_hex` | converts two ASCII hex chars into one byte      | `(ascii_lo, ascii_hi)` | `r0` = converted byte                     | `r0`, `r1`, `r2`, `r3`, SP       | `SHIFT_AMT`, `TWO_TMP`           | uses internal converter; sets `SHIFT_AMT = 4` during conversion      |
